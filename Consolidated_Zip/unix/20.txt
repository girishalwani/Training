#When a directory is created it has default link count of 2.
Because for every directory a link gets created in the parent directory
 to its new directory. 


#command to view links

ls -lrt | grep ^l

